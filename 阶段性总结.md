基于当前仓库、[paper draft](/root/ICLPose/docs/paper_draft_overview.md)、[feature workflow](/root/ICLPose/feature_field/docs/FEATURE_RECONSTRUCTION_WORKFLOW.md)、[adaptive compression plan](/root/ICLPose/自适应特征压缩PLAN.md) 和 [localization handoff](/root/ICLPose/logs/agent_handoff_dcff_localization_20260501.md)，我的判断是：

**总体判断**
当前项目的“研究动机”和“系统方向”是自洽的，但“论文级强结论”还不够稳。合理主线应该是：用 RADIO 提供强教师先验，通过 adaptive dual-space 压缩构建 map/query 共享 embedding，再让 DCFF feature field 服务于定位，而不是只追求 feature reconstruction cosine。

完成度估计：

| 维度 | 完成度 | 判断 |
|---|---:|---|
| 工程链路 | 70% | teacher、DCFF、query student、matcher、pose diagnostic 都已具备 |
| 方法闭环 | 60% | map-query joint 已成形，但 pose refine 主链路还没完全打通 |
| 实验证据 | 40% | 有局部正结果，但真实 query 厘米级定位还没证明 |
| 投稿成熟度 | 45-55% | 若目标是“厘米级定位”，证据不足；若目标是“定位导向特征场框架+瓶颈分析”，可继续打磨 |

**论文逻辑链**
当前最合理的论文叙事是：

- 动机：传统视觉定位依赖原图/稀疏特征/高维 dense teacher，隐私、存储和部署成本高；需要一个可渲染、可压缩、可定位的 3D feature field。
- 问题：高维 RADIO 特征不能直接塞进 3D field；PCA64 不是面向定位学习出来的空间；map feature 和 query student 容易各学各的；单纯 cosine 重建不等于可定位。
- 核心方法：adaptive RADIO teacher 将 1280d 压到 fine 96d / coarse 32d；DCFF 负责 map-side feature rendering；query student 输出同维度 fine/coarse；GSFF 式 query-map 对齐和 depth-aware local matcher 让特征服务于 correspondence。
- 代码实现：`feature_field` 负责 adaptive teacher + DCFF map；`feature_extract` 负责 query student 和 map-query losses；`pose_refine` 负责 depth/WLS/diagnostic。
- 创新点：adaptive channel compression、dual fine/coarse map-query feature space、定位导向 depth-aware matcher、从 feature reconstruction 到 pose refinement 的闭环诊断。

这里需要明确一点：项目不能只声称“重建了 RADIO 特征”。更强、更合理的 claim 是“面向定位的特征重建”，即重建目标、query student 和 pose solver 是共同设计的。

**当前证据**
已有结果支持三件事：

- Adaptive v7 map reconstruction 已经不是纯 PCA64 cached 路线：DCFF 日志显示 `Teacher: online_bottleneck, fine_dim=96, coarse_dim=32`，map 侧 adaptive teacher 已实现。
- DCFF map 自身有厘米级潜力：self-render query 下 corr-WLS 可到约 `0.052deg / 12.5mm`，说明 map feature field + depth/WLS 不是主要瓶颈。
- Depth-aware local matcher 有正向提升：无 matcher 的 query peak_acc 约 `0.004`，matcher v1 validation 提到 `peak_acc=0.565`，corr-WLS 约 `42.5mm`。

但目前还不能支撑“真实 query 厘米级定位”：

- 真实 query 诊断仍在约 `70-90mm` 量级，rank@1/2/5cm 基本很弱。
- LoFTR/rematch 旧路线能到约 `177.9mm` 可复现，历史 `166mm` 还未完全复现；它不是 DCFF-only 主贡献。
- Descriptor-only contrastive 已经证明不够，v27-v30 多轮都能降低 soft EPE，但 hard peak 不成立。
- query student 和 pose_refine 还没有形成完整训练闭环，当前 matcher 更多是 feature_extract 训练和 diagnostic 使用，还不是最终 runtime 主模块。

**主要不自洽点**
现在最需要收紧的是这些地方：

- 论文草稿仍混合旧 LoFTR/rematch、PCA64、oracle retrieval 和新 adaptive DCFF-only 主线，容易造成 claim 不清。
- “coarse/fine”的作用需要重定义：coarse 不必锐利，主要负责语义/候选/大 basin；fine 才负责高频局部对应和 WLS 精修。
- Adaptive teacher 目前 map 侧成立，但 query 侧多处仍依赖已导出的 adaptive feature 或 checkpoint；还不是完全统一的 online teacher-map-query end-to-end。
- 实验指标还偏 reconstruction cosine 和可视化，论文最终必须以 pose 指标为主：median t/r、rank@cm、WLS gain、flow EPE、peak_acc。
- 当前代码/配置有很多未提交改动和探针配置，投稿前必须固定 mainline config、checkpoint、复现实验命令和结果表。

**改进优先级**
下一步不建议继续单纯调 DCFF 重建 loss。优先级应是：

1. 把 depth-aware matcher 正式接入 pose_refine runtime/training，输出 flow + confidence，并用 WLS/GN pose loss 训练，而不是只在 diagnostic 里用。
2. 以 frozen adaptive v7 map 为上限，先把 real query-to-map matching 做到稳定 `peak_acc` 和 `rank@5/10cm` 提升，再考虑解冻 map。
3. 重新组织实验表：PCA64 baseline、adaptive 96/32、adaptive+query-map、adaptive+depth matcher、self-render upper bound、LoFTR/rematch reference。
4. 可视化不再只放 PCA feature map，要加 correlation heatmap、flow error、confidence map、WLS update direction，这些更能解释定位成败。
5. 论文主线改成“localization-oriented feature field reconstruction”，不要把旧 LoFTR oracle 作为核心贡献，只作为 reference/upper-bound baseline。
6. 等 real query 达到稳定亚 5cm 后，再低学习率解冻 fine decoder / latent；geometry 最后再碰，避免把问题再次混到重建几何里。

最终投稿目标如果坚持“厘米级定位”，当前还差最关键一环：真实 query 到 DCFF map 的可泛化局部对应。现在最强证据已经说明 map 端上限存在，下一步应该集中证明 query-map matcher 能把这个上限吃到，而不是继续证明 map 特征能被可视化得更像 teacher。