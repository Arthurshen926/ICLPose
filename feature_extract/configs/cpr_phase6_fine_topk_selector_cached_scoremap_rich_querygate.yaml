# Cached score-map selector/export config that keeps the Phase6 coarse scorer
# but enables the A4 query-side channel gate for query feature evidence export.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6_fine_topk_selector_cached_scoremap_rich.yaml
exp_name: cpr_phase6_fine_topk_selector_cached_scoremap_rich_querygate

model:
  query_channel_gate_enabled: true
  apply_query_channel_gate: true
  query_channel_gate_hidden_dim: 64
  query_channel_gate_zero_init: true
