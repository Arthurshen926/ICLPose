# Phase 5A: safe map-side fine-tune probe.
# Query/scorer stay frozen; only map features are updated against the fixed
# query feature space at the GT render pose. Candidate-bank gradients are kept
# off here because full K169 candidate rendering with map gradients OOMs.

base_config: /root/ICLPose/feature_extract/configs/cpr_clean_phase4_v13_coarse_lattice_probe_medium_combo_rankhard_batch32_b6.yaml
exp_name: cpr_clean_phase5_maponly_gtalign_medium_b4

dataset:
  max_train_samples: 64
  max_val_samples: 32

training:
  batch_size: 4
  max_steps: 128
  log_every: 2
  lr: 0.0002
  best_metric: map_query_fine_loss
  best_metric_mode: min
  freeze_model_except_prefixes:
    - "__no_model_params__"

map_supervision:
  trainable: true
  train_geometry: false
  train_color: false
  train_fine_decoder: true
  train_coarse_fusion: true
  train_feat_sharp: false
  train_fsm: false
  train_hash_mlp: true
  train_latent: true
  map_lr_scale: 0.02
  hash_mlp_lr_scale: 0.01
  latent_lr_scale: 0.003
  detach_query_features: true

  candidate_render_score_train_map: false
  candidate_render_batch_size: 0
  candidate_render_score_weight: 0.0
  candidate_score_fusion_weight: 0.0
  candidate_refined_pose_weight: 0.0

  query_fine_weight: 0.10
  query_coarse_weight: 0.05
  query_corr_ce_weight: 0.0
  query_corr_subpixel_weight: 0.0
  query_corr_flow_cosine_weight: 0.0
  query_corr_peak_margin_weight: 0.0
  query_corr_wls_pose_weight: 0.0
  rendered_teacher_fine_weight: 0.0005
  rendered_teacher_coarse_weight: 0.0005
