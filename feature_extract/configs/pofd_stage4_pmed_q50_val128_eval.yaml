# Eval-only companion for POFD Stage4 PMED q50.
#
# Keeps the Stage4 r12 pair-matcher score path and lifts the inherited
# validation cap to the full q50 val128 candidate cache.

base_config: /root/ICLPose/feature_extract/configs/pofd_stage4_pmed_q50_shuf.yaml
exp_name: pofd_stage4_pmed_q50_val128_eval

dataset:
  max_val_samples: 128
  teacher_correspondence_val_path:

