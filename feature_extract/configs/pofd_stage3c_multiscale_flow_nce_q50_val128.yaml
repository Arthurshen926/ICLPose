# POFD Stage 3c probe: keep the Stage3a pair-matcher score surface and add
# geometry-targeted local-flow NCE on the same low-resolution scoring grid.
#
# This is the minimal implementation of the expert recommendation that NVS
# supervision should shape correlation peaks, not just feature appearance.

base_config: /root/ICLPose/feature_extract/configs/pofd_stage3a_multiscale_score_q50_val128_eval.yaml
exp_name: pofd_stage3c_multiscale_flow_nce_q50_val128

nvs_pose_feature_adapter:
  local_flow_nce_weight: 0.1
  local_corr_radius: 12
  local_corr_temperature: 0.07

