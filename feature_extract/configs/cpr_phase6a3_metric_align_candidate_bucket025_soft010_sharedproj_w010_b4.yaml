# A3 soft-target variant: same 0.25m/5deg candidate basin as A3, but
# trains render-score evidence against a soft GT pose-error distribution
# instead of a single hard nearest-candidate label.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6a3_metric_align_candidate_bucket025_sharedproj_w010_b4.yaml
exp_name: cpr_phase6a3_metric_align_candidate_bucket025_soft010_sharedproj_w010_b4

training:
  max_steps: 128

map_supervision:
  candidate_render_score_target_mode: gt_pose_error_soft
  candidate_render_score_target_temperature_m: 0.10
