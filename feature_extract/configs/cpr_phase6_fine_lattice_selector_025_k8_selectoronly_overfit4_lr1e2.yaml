# Diagnostic: can the current fine selector memorize top8 ranking on four samples?
# If this does not drive train fsel/gap down, the selector evidence or loss path is
# broken/insufficient, not merely under-trained.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6_fine_lattice_selector_025_k8_selectoronly_peak_fullctx_hard_lr2e3_b4_fast.yaml
exp_name: cpr_phase6_fine_lattice_selector_025_k8_selectoronly_overfit4_lr1e2

dataset:
  max_train_samples: 4
  max_val_samples: 4

training:
  lr: 0.01
  batch_size: 4
  max_steps: 128
  freeze_model_except_prefixes:
    - "fine_candidate_selector_head."
