# Cached score-map selector/export config with RGB difference statistics.
# This is a diagnostic for whether direct image evidence is more pose-aware
# than the current query-map fine correlation in K64 reranking.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6_fine_topk_selector_cached_scoremap_rich.yaml
exp_name: cpr_phase6_fine_topk_selector_cached_scoremap_rich_rgb

model:
  fine_candidate_selector_use_rgb_features: true

map_supervision:
  fine_topk_selector_use_rgb: true
