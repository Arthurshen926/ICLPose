# Clean CPR Phase 2 projector-only run with larger batch size.
# Same objective as b20, higher memory utilization and smoother validation.

base_config: /root/ICLPose/feature_extract/configs/cpr_clean_phase2_v13_corrproj_b20.yaml
exp_name: cpr_clean_phase2_v13_corrproj_b32

training:
  batch_size: 32
  num_workers: 0
  max_steps: 3000
  lr: 0.0002

