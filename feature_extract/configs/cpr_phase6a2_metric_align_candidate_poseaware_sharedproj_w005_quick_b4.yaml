# Quick A2 diagnostic with K64 candidate-level pose-aware supervision.
# Batch 4 is the current throughput sweet spot for sequential fine candidate
# rendering on 24GB GPUs.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6a2_metric_align_candidate_poseaware_sharedproj_w005_b4.yaml
exp_name: cpr_phase6a2_metric_align_candidate_poseaware_sharedproj_w005_quick_b4

dataset:
  max_train_samples: 96
  max_val_samples: 32

training:
  batch_size: 4
  validation_batch_size: 2
  max_steps: 160
