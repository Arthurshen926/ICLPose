# Phase6-B full-data scene-coordinate diagnostic.  The quick 192-sample run
# learned only meter-level coordinates, so this uses the full train split with
# a larger batch before judging the geometry-head route.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6b_scene_coord_head_frozen_b8.yaml
exp_name: cpr_phase6b_scene_coord_head_frozen_full_b32

dataset:
  max_train_samples: null
  max_val_samples: 64

training:
  batch_size: 32
  validation_batch_size: 8
  max_steps: 800
  log_every: 25
