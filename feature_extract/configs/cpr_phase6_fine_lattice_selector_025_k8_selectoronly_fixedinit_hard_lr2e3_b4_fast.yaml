# Phase 6 diagnostic/main candidate: top8 selector with deterministic per-image
# T0 perturbations, so the fine top8 supervision is stable across epochs.

base_config: /root/ICLPose/feature_extract/configs/cpr_phase6_fine_lattice_selector_025_k8_selectoronly_peak_fullctx_hard_lr2e3_b4_fast.yaml
exp_name: cpr_phase6_fine_lattice_selector_025_k8_selectoronly_fixedinit_hard_lr2e3_b4_fast

training:
  batch_size: 4
  max_steps: 96
  freeze_model_except_prefixes:
    - "fine_candidate_selector_head."

map_supervision:
  perturb_deterministic_by_name: true
  perturb_seed: 20260510
  fine_topk_selector_topk: 8
